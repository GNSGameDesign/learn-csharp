using System; // this is the namespace that contains the Console class

class Program // in C# you do not necessarily have to name the same thing as the file name
{
      static void Main() // notice here how we don't have to specify the parameters as compared to Java
      {
            Console.WriteLine("Hello, World!"); // this is similar to Java's System.out.println()
      }
}